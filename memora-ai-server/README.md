Run `.venv\Scripts\Activate.ps1` to activate virtual environment.
Run `deactivate` to deactivate the virtual environment.

Run `python -r requirements.txt` to install dependencies in the requirements.txt file